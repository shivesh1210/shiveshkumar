<ul class="card-text font-weight-light list-group list-group-flush">
  {% assign talks = data[1] | sort: 'date' | reverse %}
  {% for content in talks %}
    <li class="list-group-item">
      <div class="row">
        <div class="col-xs-2 cl-sm-2 col-md-2 text-left date-column">
          {% if content.date %}
            {% assign date = content.date | split: '-' | slice: 0, 2 | join: '.' %}
          {% else %}
            {% assign date = '' %}
          {% endif %}
          <table class="table-cv">
            <tbody>
              <tr>
                <td>
                  <span
                    class="badge font-weight-bold text-uppercase align-left"
                    style="min-width: 75px; background-color: black !important; color: white;"
                  >
                    {{ date }}
                  </span>
                </td>
              </tr>
              {% if content.location %}
                <tr>
                  <td>
                    <p class="location">
                      <i class="fa-solid fa-location-dot iconlocation"></i>
                      {{ content.location }}
                    </p>
                  </td>
                </tr>
              {% endif %}
            </tbody>
          </table>
        </div>
        <div class="col-xs-10 cl-sm-10 col-md-10 mt-2 mt-md-0">
          <h6 class="title font-weight-bold ml-1 ml-md-4">
            <a href="{{ content.url }}">{{ content.title }}</a>
          </h6>
          <h6 class="ml-1 ml-md-4" style="font-size: 0.95rem">{{ content.event }}</h6>
          <h6 class="ml-1 ml-md-4" style="font-size: 0.90rem">{{ content.organization }}</h6>
          <h6 class="ml-1 ml-md-4" style="font-size: 0.81rem; font-style: italic">{{ content.summary }}</h6>
          <ul class="items">
            {% for item in content.notes %}
              <li>
                <span class="item">{{ item }}</span>
              </li>
            {% endfor %}
          </ul>
        </div>
      </div>
    </li>
  {% endfor %}
</ul>
